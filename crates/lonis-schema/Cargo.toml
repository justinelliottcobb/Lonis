[package]
name = "lonis-schema"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Shared contract layer for Lonis-compatible tools (envelope, capabilities, tool errors, tool contracts)"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sha2 = { workspace = true }
time = { workspace = true }
lonis-identity = { workspace = true }
lonis-derive = { workspace = true, optional = true }

[features]
default = []
derive = ["dep:lonis-derive"]

[dev-dependencies]
jsonschema = "0.33.0"
